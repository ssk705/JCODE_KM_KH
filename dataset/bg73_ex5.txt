[CODE]  
public class LoopExample5 {  
    public static void main(String[] args) {  
        int j = 0;  
        for (int i = 0; i < 3; i++) {  
            while (j < 2) {  
                System.out.println("Inner");  
                j++;  
            }  
            System.out.println("Outer");  
        }  
    }  
}  
[OUTPUT]  
KM: The inner `while` loop uses `j`, which is not reset for each outer loop iteration, causing the inner loop to run only once.  
KH: Reset `j` inside the outer loop (e.g., `j = 0`) before the inner loop to ensure it runs in every outer iteration.