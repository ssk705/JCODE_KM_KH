[CODE]
public class GradeEvaluator {
    public static void main(String[] args) {
        char grade = 'B';
        switch (grade) {
            case 'A':
                System.out.println("Excellent!");
            case 'B':
                System.out.println("Good job!");
            case 'C':
                System.out.println("Average.");
            default:
                System.out.println("Needs improvement.");
        }
    }
}
[OUTPUT]
KM:Missing `break` after each `case`, causing unintended "fall-through" behavior.
KH:Add `break;` after each case's logic to terminate the switch block after matching a case.  