[CODE]
class SuperClass {
    public static void print() {
        System.out.println("Super");
    }
}

class SubClass extends SuperClass {
    public static void print() {
        System.out.println("Sub(");
    }
} 
[OUTPUT]
KM:Declaring a `static` method with the same name as a superclass `static` method does **not** override it; it *hides* it.
KH:Polymorphism doesn’t apply. Calling `print()` via a `SuperClass` reference always invokes `SuperClass.print()`, even if the object is a `SubClass`.  