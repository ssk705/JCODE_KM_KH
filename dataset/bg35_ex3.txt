[CODE]
class Animal {
    public void speak() {
        System.out.println("Animal sound");
    }
}

class Dog extends Animal {
    public void speak(String sound) {
        System.out.println(sound);
    }
}
[OUTPUT]
KM:The `Dog` class declares `speak(String)` instead of matching the superclass method `speak()` exactly. This creates an overloaded method, not an overridden one.  
KH:To override a superclass method, ensure the method signature (name and parameters) in the subclass exactly matches the superclass method. Use `@Override` annotation to enforce this. 