[CODE]  
public class LoopExample1 {  
    public static void main(String[] args) {  
        int[] arr = {1, 2, 3};  
        for (int i = 0; i <= arr.length; i++) {  
            System.out.println(arr[i]);  
        }  
    }  
}  
[OUTPUT]  
KM: The loop condition uses `i <= arr.length`, which causes an `ArrayIndexOutOfBoundsException` because array indices go up to `arr.length - 1`.  
KH: Change the loop condition to `i < arr.length` to ensure the index stays within valid bounds.  