[CODE]
class Point {
    int x, y;
    Point(int x, int y) { this.x = x; this.y = y; }
}
public class Bg172 {
    public static void main(String[] args) {
        Point p1 = new Point(1, 2);
        Point p2 = p1;
        p2.x = 5;
        System.out.println(p1.x);  
    }
}
[OUTPUT]
KM:`p2 = p1` copies the reference, not the object. Both share the same `Point` instance.  
KH: Implement a copy constructor or cloning method to duplicate the object.  
