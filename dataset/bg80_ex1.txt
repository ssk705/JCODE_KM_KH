[CODE]  
public class Bg801 {  
    public static void main(String[] args) {  
        String s = "test";  
        for (int i = 0; i < s.length; i++) {  
            System.out.println(s.charAt(i));  
        }  
    }  
}  
[OUTPUT]  
KM: Using `.length` as a field for a String, which is incorrect because Strings require the method `.length()`.  
KH: Replace `s.length` with `s.length()` to properly access the length of the String.  
