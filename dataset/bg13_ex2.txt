[CODE]
public class Car {
    String model;
    public Car(String m) { model = m; }
}

public class Garage {
    public static void main(String[] args) {
        Car c1 = new Car("Sedan");
        Car c2 = new Car(c1);  
    }
}
[OUTPUT]
KM:`Car` has no copy constructor, so `new Car(c1)` is invalid. Constructors are not inherited.  
KH:Define a copy constructor in `Car` to initialize a new object using an existing `Car` instance.