[CODE]  
public class Bg162 {  
    public static int findIndex(int[] arr, int target) {  
        for (int i = 0; i < arr.length; i++) {  
            if (arr[i] == target) {  
                return i;  
            }  
        }  
    }  
}  
[OUTPUT]  
KM: If the target is not found in the array, the loop completes without returning a value, causing the method to exit without returning an int.  
KH: Add a return statement after the loop (e.g., return -1;) to handle the case where the target is not present.  