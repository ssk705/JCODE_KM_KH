[CODE]
public class ArrayStack {
    private int[] stack;
    private int top;
    
    public ArrayStack(int capacity) {
        stack = new int[capacity];
        top = -1;
    }
    
    public int peek() {
        return stack[top];
    }
}
[OUTPUT]
KM: The `peek()` method accesses `stack[top]` without checking if the stack is empty. When empty (`top = -1`), this throws `ArrayIndexOutOfBoundsException`.  
KH: Add an underflow check in `peek()` (e.g., `if (isEmpty()) throw new EmptyStackException();`).