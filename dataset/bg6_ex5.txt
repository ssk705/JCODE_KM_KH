[CODE]  
public class Bg65 {  
    public static void main(String[] args) {  
        int temp = 25;  
        if { temp > 30 } {  
            System.out.println("It's hot!");  
        }  
    }  
}  
[OUTPUT]  
KM: The `if` statement incorrectly uses curly braces `{}` to enclose the condition instead of parentheses `()`, causing a syntax error.  
KH: Replace the curly braces `{}` surrounding the condition with parentheses `()` to define the `if` statement correctly.  
